"""MCP client layer — NOT YET IMPLEMENTED.

client.py will speak MCP over stdio + streamable-http; servers.yaml registers servers
(playwright, safeai, ...). All tool calls route through the permission broker.
"""
